package dss.repository;

import dss.model.entity.QuestionnaireDimensionScore;
import org.springframework.data.jpa.repository.JpaRepository;

public interface QuestionnaireDimensionScoreRepository extends JpaRepository<QuestionnaireDimensionScore, Long> {
}
